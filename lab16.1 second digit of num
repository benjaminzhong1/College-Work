#include <iostream>
using namespace std;
int secondDigit(int n)
  {
  if (n>=10 && n<100) return n%10;
  return secondDigit(n/10);
  }
int main() {
  int n;
  cout<<"enter a number n\n";
  cin>>n;
  cout<<secondDigit(n)<<endl;
}
